function postCard(post){return `<article class="post-card"><time datetime="${post.date}">${post.displayDate}</time><h3><a href="${post.url}">${post.title}</a></h3><p>${post.summary}</p><a class="text-link" href="${post.url}">Read note →</a></article>`}function postList(post){return `<article><time datetime="${post.date}">${post.displayDate}</time><h2><a href="${post.url}">${post.title}</a></h2><p>${post.summary}</p><a class="text-link" href="${post.url}">Read note →</a></article>`}const posts=(window.BLOG_POSTS||[]).slice().sort((a,b)=>new Date(b.date)-new Date(a.date));document.querySelectorAll('[data-latest-posts]').forEach(el=>el.innerHTML=posts.slice(0,3).map(postCard).join(''));document.querySelectorAll('[data-all-posts]').forEach(el=>el.innerHTML=posts.map(postList).join(''));