const blogScript=[...document.scripts].find(s=>s.src.endsWith('/blog.js')||s.src.endsWith('blog.js'));const siteRoot=blogScript?new URL('../../',blogScript.src):new URL('./',document.baseURI);const postHref=p=>new URL(p.path,siteRoot).href;function postCard(p){const href=postHref(p);return `<article class="post-card"><time datetime="${p.date}">${p.displayDate}</time><h3><a href="${href}">${p.title}</a></h3><p>${p.summary}</p><a class="text-link" href="${href}">Read note →</a></article>`}function postList(p){const href=postHref(p);return `<article><time datetime="${p.date}">${p.displayDate}</time><h2><a href="${href}">${p.title}</a></h2><p>${p.summary}</p><a class="text-link" href="${href}">Read note →</a></article>`}const posts=(window.BLOG_POSTS||[]).slice().sort((a,b)=>new Date(b.date)-new Date(a.date));document.querySelectorAll('[data-latest-posts]').forEach(el=>el.innerHTML=posts.slice(0,3).map(postCard).join(''));document.querySelectorAll('[data-all-posts]').forEach(el=>el.innerHTML=posts.map(postList).join(''));