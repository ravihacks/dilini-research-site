function postCard(p){return `<article class="post-card"><time datetime="${p.date}">${p.displayDate}</time><h3><a href="${p.url}">${p.title}</a></h3><p>${p.summary}</p><a class="text-link" href="${p.url}">Read note →</a></article>`}function postList(p){return `<article><time datetime="${p.date}">${p.displayDate}</time><h2><a href="${p.url}">${p.title}</a></h2><p>${p.summary}</p><a class="text-link" href="${p.url}">Read note →</a></article>`}const posts=(window.BLOG_POSTS||[]).slice().sort((a,b)=>new Date(b.date)-new Date(a.date));document.querySelectorAll('[data-latest-posts]').forEach(el=>el.innerHTML=posts.slice(0,3).map(postCard).join(''));document.querySelectorAll('[data-all-posts]').forEach(el=>el.innerHTML=posts.map(postList).join('')); 